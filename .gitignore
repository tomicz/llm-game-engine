# Binary (root only; do not ignore cmd/game/)
/game
# Built game binary
cmd/game/game

# Logs (generated at runtime)
logs/
terminal.txt

# Engine config (user preferences, generated on first toggle)
config/

# Environment (API keys; copy .env.example to .env and fill in)
.env

# Fonts downloaded from URL (cmd font url <url>); kept locally, not committed
assets/fonts/downloaded/
